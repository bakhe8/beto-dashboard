name: BetoDashboard CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  test:
    name: Run Test Suite
    uses: ./.github/workflows/test.yml
    # This workflow does not require any secrets
    secrets: inherit